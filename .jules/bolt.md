## 2024-05-22 - [Image Optimization Attributes]
**Learning:** Even without image conversion tools, adding explicit `width`, `height`, `loading="lazy"`, and `decoding="async"` attributes significantly improves LCP and CLS.
**Action:** Always check for missing image attributes in static HTML files, especially for "above the fold" vs "below the fold" strategies.
